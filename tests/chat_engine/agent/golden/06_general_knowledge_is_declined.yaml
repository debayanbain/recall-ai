name: who is Sunny Leone
why: >
  The message a live bot answered with a biography. There is no gate in front of the model
  any more, so this case is the prompt doing the work -- and the guard's length cap is
  what still holds if the prompt is talked past.
user_text: "Who is sunny leone?"
snapshot: []
model_turns:
  - calls:
      - tool: FinalAnswer
        args:
          text: "I can only help with what you've saved here. Send me a link or a note and I'll keep it, or ask me what's in your vault."
          declined_out_of_scope: true
expect:
  tool_calls: [FinalAnswer]
  reply_not_contains: ["actress", "Sunny Leone is"]
  declined: true
  max_chars: 600
  failed: false
